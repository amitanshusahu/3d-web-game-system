export const presetMapsKnowledge = {
  floating_ancient: {
    mobileHome:
      'a floating fortress in the sky, with a mobile home on top. a home that acts as bridge from one part to another with mechanical moving parts.',
    strongHold:
      'the last stronghold is a floating srine in the sky, with bridges wobling in the wind. player needs to cross the bridges to reach the shrine.',
  },
};

// Backwards-compatible alias (fixes typo in old export name).
export const presetMapsKonwledge = presetMapsKnowledge;
